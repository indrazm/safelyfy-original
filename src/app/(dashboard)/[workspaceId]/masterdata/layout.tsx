import { MasterdataLayout } from "@/components/App/MainApp/Masterdata/Layout"

export default function Layout({ children }: childrenProps) {
    return <MasterdataLayout>{children}</MasterdataLayout>
}
